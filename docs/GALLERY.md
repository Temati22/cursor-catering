# Галерея - Gallery Page

Документация по работе с галереей фотографий на сайте Hi-Catering.

## 📋 Обзор

Страница галереи (`/galery`) позволяет отображать коллекцию фотографий ваших мероприятий и блюд. Контент управляется через админ-панель Strapi.

## 🏗️ Структура контент-типа

### Поля Gallery:

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `title` | String | Да | Заголовок страницы галереи |
| `description` | Text | Нет | Описание галереи |
| `images` | Media (Multiple) | Нет | Коллекция фотографий |
| `SEO` | Component (shared.seo) | Нет | SEO метаданные |

### Тип контента:
- **Kind:** `singleType` (одна страница галереи)
- **Draft & Publish:** Включено

## 🚀 Быстрый старт

### 1. Запустите Strapi backend

```bash
cd backend
npm run develop
```

### 2. Настройте права доступа

Выполните одно из действий:

**Опция A: Автоматическая инструкция**
```bash
node backend/scripts/setup-gallery-permissions.js
```

**Опция B: Ручная настройка**
1. Откройте админ-панель: `http://localhost:1337/admin`
2. Перейдите: `Settings → Users & Permissions plugin → Roles → Public`
3. Найдите `Gallery` в списке прав
4. Включите галочку `find` (чтение данных)
5. Нажмите `Save`

### 3. Добавьте контент

1. В админ-панели Strapi перейдите в: `Content Manager → Gallery`
2. Заполните поля:
   - **Title**: Например, "Галерея Hi-Catering"
   - **Description**: Краткое описание галереи
   - **Images**: Загрузите фотографии
   - **SEO** (опционально): Настройте SEO метаданные
3. Нажмите `Save`
4. Нажмите `Publish`

### 4. Запустите frontend

```bash
cd apps/web
npm run dev
```

### 5. Откройте страницу

Страница доступна по адресу: `http://localhost:3000/galery`

## 📁 Файловая структура

### Backend (Strapi)

```
backend/src/api/gallery/
├── content-types/
│   └── gallery/
│       └── schema.json          # Схема контент-типа
├── controllers/
│   └── gallery.ts               # Контроллер API
├── routes/
│   └── gallery.ts               # Маршруты API
└── services/
    └── gallery.ts               # Сервисный слой
```

### Frontend (Next.js)

```
apps/web/src/
├── app/
│   └── galery/
│       └── page.tsx             # Страница галереи
└── lib/
    └── api.ts                   # API клиент (типы и функция getGallery)
```

## 🔧 API

### Endpoint

```
GET /api/gallery
```

### Пример запроса

```javascript
import apiClient from '@/lib/api';

const gallery = await apiClient.getGallery();
console.log(gallery.data);
```

### Пример ответа

```json
{
  "data": {
    "id": 1,
    "documentId": "xxx",
    "title": "Галерея Hi-Catering",
    "description": "Фотографии наших мероприятий",
    "images": [
      {
        "id": 1,
        "url": "/uploads/photo1.jpg",
        "name": "photo1.jpg",
        "alternativeText": "Корпоративное мероприятие",
        "width": 1920,
        "height": 1080,
        "formats": {
          "thumbnail": { "url": "/uploads/thumbnail_photo1.jpg" },
          "medium": { "url": "/uploads/medium_photo1.jpg" },
          "large": { "url": "/uploads/large_photo1.jpg" }
        }
      }
    ],
    "SEO": {
      "metaTitle": "Галерея | Hi-Catering",
      "metaDescription": "Фотографии наших мероприятий и блюд",
      "metaKeywords": "кейтеринг, галерея, фото"
    },
    "createdAt": "2025-11-07T10:00:00.000Z",
    "updatedAt": "2025-11-07T10:00:00.000Z",
    "publishedAt": "2025-11-07T10:00:00.000Z"
  }
}
```

## 🎨 UI/UX Features

- **Адаптивная сетка**: 1 колонка на мобильных, 2 на планшетах, 3 на десктопах
- **Hover эффекты**: Увеличение изображения и отображение подписи
- **Оптимизация изображений**: Автоматическое использование форматов medium/small
- **Lazy loading**: Отложенная загрузка изображений (первые 6 загружаются сразу)
- **SEO**: Metadata и Open Graph теги
- **Breadcrumbs**: Хлебные крошки для навигации

## 📱 Навигация

Ссылки на галерею добавлены в:

1. **Header** (`/components/Header.tsx`):
   - Основная навигация
   - Путь: `/galery`

2. **Footer** (`/components/Footer.tsx`):
   - Раздел "Быстрые ссылки"
   - Путь: `/galery`

## 🔍 SEO

Базовые SEO настройки:

```typescript
export const metadata: Metadata = {
    title: 'Галерея | Hi-Catering',
    description: 'Галерея фотографий наших мероприятий и блюд',
    openGraph: {
        title: 'Галерея | Hi-Catering',
        description: 'Галерея фотографий наших мероприятий и блюд',
        type: 'website',
    },
};
```

Вы можете настроить дополнительные SEO параметры через компонент SEO в Strapi.

## 🛠️ Кастомизация

### Изменение количества колонок

В файле `apps/web/src/app/galery/page.tsx`:

```tsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
  {/* Измените классы: 
      - grid-cols-1: мобильные
      - sm:grid-cols-2: планшеты
      - lg:grid-cols-3: десктопы
  */}
</div>
```

### Изменение стиля карточек

```tsx
<div className="... aspect-square ...">
  {/* aspect-square - квадратные карточки
      aspect-video - 16:9
      aspect-[4/3] - 4:3
  */}
</div>
```

### Добавление фильтров

Можно добавить категории или теги в схему и реализовать фильтрацию на frontend.

## 🐛 Troubleshooting

### Ошибка 403 (Forbidden)

**Причина:** Не настроены права доступа

**Решение:** Выполните шаг 2 из "Быстрый старт"

### Изображения не отображаются

**Причина:** Неправильный путь к изображениям

**Решение:** Проверьте переменные окружения:
- `NEXT_PUBLIC_STRAPI_URL` в `apps/web/.env`
- `NEXT_PUBLIC_API_URL` в `apps/web/.env`

### Галерея пустая

**Причина:** Контент не опубликован или не добавлен

**Решение:** 
1. Проверьте, что контент опубликован (кнопка `Publish` в Strapi)
2. Убедитесь, что загружены изображения

## 📝 TODO / Future Enhancements

- [ ] Добавить lightbox для просмотра изображений в полном размере
- [ ] Категории галереи (события, блюда, интерьеры и т.д.)
- [ ] Фильтры и поиск
- [ ] Пагинация для большого количества фотографий
- [ ] Видео галерея
- [ ] Instagram интеграция

## 📞 Support

При возникновении проблем:
1. Проверьте логи Strapi: `backend/.tmp/logs/`
2. Проверьте логи Next.js в консоли браузера
3. Убедитесь, что все зависимости установлены
4. Перезапустите сервисы

---

**Создано:** 07.11.2025  
**Версия:** 1.0.0

