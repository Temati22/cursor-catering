# 🔧 Исправление ошибки 403 для страницы Галереи

## ❌ Проблема

При открытии страницы `/galery` возникает ошибка:
```
AxiosError: Request failed with status code 403
```

## ✅ Решение

Ошибка возникает потому, что права доступа к API галереи не настроены. Есть 2 способа исправить это:

---

## 🚀 Способ 1: Автоматическая настройка (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Остановите Strapi

Если Strapi запущен (`npm run develop`), остановите его (Ctrl+C).

**⚠️ ВАЖНО:** Скрипт должен запускаться когда Strapi НЕ работает!

### Шаг 2: Запустите скрипт настройки прав

```bash
cd backend
npm run setup:gallery-permissions
```

Или можно настроить права для ВСЕХ контент-типов сразу:

```bash
cd backend
npm run setup:permissions
```

### Шаг 3: Запустите Strapi снова

```bash
npm run develop
```

### Шаг 4: Перезагрузите страницу

Обновите страницу `/galery` в браузере.

---

## 🔧 Способ 2: Ручная настройка через админ-панель

### Шаг 1: Откройте админ-панель Strapi

```
http://localhost:1337/admin
```

### Шаг 2: Перейдите в настройки прав

```
Settings → Users & Permissions plugin → Roles → Public
```

### Шаг 3: Найдите Gallery

Прокрутите список до раздела "Gallery"

### Шаг 4: Включите права

Поставьте галочку ✅ напротив:
- **find** (позволяет читать данные галереи)

### Шаг 5: Сохраните

Нажмите кнопку **Save** в правом верхнем углу

### Шаг 6: Перезагрузите страницу

Обновите страницу `/galery` в браузере.

---

## ✅ Проверка

После настройки прав:

1. **API работает:**
   ```
   http://localhost:1337/api/gallery
   ```
   Должен вернуть JSON с данными (или `{"data": null}` если контент не добавлен)

2. **Страница работает:**
   ```
   http://localhost:3000/galery
   ```
   Должна открыться без ошибок

---

## 📝 Следующий шаг: Добавьте контент

Теперь, когда права настроены, добавьте контент в галерею:

1. Откройте админ-панель Strapi: `http://localhost:1337/admin`
2. Перейдите: `Content Manager → Gallery`
3. Заполните поля:
   - **Title**: "Галерея Hi-Catering"
   - **Description**: "Фотографии наших мероприятий и блюд"
   - **Images**: Загрузите фотографии
4. Нажмите `Save`
5. Нажмите `Publish`
6. Обновите страницу `/galery`

---

## 🐛 Если ошибка сохраняется

### Очистите кеш и перезапустите всё

```bash
# Остановите оба сервиса (Ctrl+C)

# Backend
cd backend
rm -rf .cache
rm -rf build
npm run develop

# В новом терминале - Frontend
cd apps/web
rm -rf .next
npm run dev
```

### Проверьте переменные окружения

В `apps/web/.env.local` или `apps/web/.env`:

```env
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
NEXT_PUBLIC_API_URL=http://localhost:1337/api
```

---

## 💡 Дополнительная информация

- Полная документация: [docs/GALLERY.md](docs/GALLERY.md)
- Быстрая настройка: [GALLERY_SETUP.md](GALLERY_SETUP.md)

---

**Готово!** После выполнения этих шагов страница галереи должна работать корректно. 🎉

