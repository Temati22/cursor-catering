# 🚀 Установка Hi-Catering на сервер

## 📋 Требования к серверу

### Минимальные параметры
- **Процессор:** 2 ядра
- **Оперативная память:** 4 GB
- **Диск:** 20 GB SSD
- **ОС:** Ubuntu 22.04 LTS или новее
- **Порты:** 80, 443, 22 (открытые)

### Рекомендуемые параметры для production
- **Процессор:** 4+ ядра
- **Оперативная память:** 8 GB
- **Диск:** 100 GB SSD
- **Автоматические резервные копии**

---

## ⚡ Быстрая установка (5 минут)

### 1. Подготовка сервера

```bash
# Обновление системы и установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
newgrp docker

# Настройка файрвола
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw --force enable
```

### 2. Установка проекта

```bash
# Клонирование проекта
cd /var/www
sudo git clone <URL_РЕПОЗИТОРИЯ> hi-catering
cd hi-catering
sudo chown -R $USER:$USER /var/www/hi-catering

# Настройка переменных окружения
cp env.example .env
nano .env  # Отредактируйте .env файл (см. раздел ниже)
```

### 3. Настройка .env файла

**Обязательно измените следующие параметры:**

```env
# Ваш домен
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
NEXT_PUBLIC_STRAPI_URL=https://api.yourdomain.com
PUBLIC_URL=https://api.yourdomain.com

# Пароли (ПРИДУМАЙТЕ СИЛЬНЫЕ ПАРОЛИ!)
DATABASE_PASSWORD=ВАША_БАЗА_ПАРОЛЬ_ТУТ
REDIS_PASSWORD=ВАШ_REDIS_ПАРОЛЬ_ТУТ

# Ключи безопасности (сгенерируйте уникальные)
APP_KEYS=КЛЮЧ1,КЛЮЧ2
API_TOKEN_SALT=ВАША_СОЛЬ
ADMIN_JWT_SECRET=ВАШ_JWT_СЕКРЕТ
TRANSFER_TOKEN_SALT=ВАША_СОЛЬ2
JWT_SECRET=ВАШ_JWT_СЕКРЕТ2
```

**Генерация безопасных ключей:**

```bash
# Запустите эту команду 5 раз для генерации 5 уникальных ключей
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

### 4. Настройка DNS

Добавьте A-записи у вашего регистратора доменов:

```
yourdomain.com      -> IP_ВАШЕГО_СЕРВЕРА
www.yourdomain.com  -> IP_ВАШЕГО_СЕРВЕРА
api.yourdomain.com  -> IP_ВАШЕГО_СЕРВЕРА
```

### 5. SSL сертификат

```bash
# Установка Certbot
sudo apt install certbot -y

# Получение бесплатного SSL от Let's Encrypt
sudo certbot certonly --standalone \
  -d yourdomain.com \
  -d www.yourdomain.com \
  -d api.yourdomain.com \
  --email your@email.com \
  --agree-tos

# Копирование сертификатов
sudo mkdir -p infra/nginx/ssl
sudo cp /etc/letsencrypt/live/yourdomain.com/fullchain.pem infra/nginx/ssl/
sudo cp /etc/letsencrypt/live/yourdomain.com/privkey.pem infra/nginx/ssl/
sudo chown -R $USER:$USER infra/nginx/ssl
```

### 6. Обновление Nginx конфигурации

```bash
# Откройте файл конфигурации
nano infra/nginx/nginx.conf

# Замените все "yourdomain.com" на ваш реальный домен
# Сохраните файл (Ctrl+O, Enter, Ctrl+X)
```

### 7. Запуск приложения

```bash
# Автоматический деплой (рекомендуется)
./scripts/deploy.sh

# ИЛИ ручной запуск
docker compose -f docker-compose.prod.yml up -d --build
```

### 8. Первая настройка

1. Дождитесь запуска всех сервисов (1-2 минуты)
2. Откройте в браузере: `https://api.yourdomain.com/admin`
3. Создайте администратора Strapi
4. Заполните начальными данными (опционально):

```bash
docker exec -it hi-catering-backend sh
npm run seed:all
npm run publish:all
npm run setup:permissions
exit
```

5. Проверьте сайт: `https://yourdomain.com`

---

## ✅ Готово! Сайт запущен!

### Что дальше?

#### Регулярные задачи

```bash
# Просмотр логов
docker compose -f docker-compose.prod.yml logs -f

# Перезапуск
docker compose -f docker-compose.prod.yml restart

# Резервное копирование
./scripts/backup.sh

# Обновление
git pull origin main
docker compose -f docker-compose.prod.yml up -d --build
```

#### Автоматическое резервное копирование

```bash
# Добавьте в cron (каждый день в 3:00)
crontab -e

# Добавьте строку:
0 3 * * * cd /var/www/hi-catering && ./scripts/backup.sh >> /var/log/backup.log 2>&1
```

#### Автообновление SSL

```bash
# Добавьте в cron
sudo crontab -e

# Добавьте строку:
0 */12 * * * certbot renew --quiet --post-hook "cp /etc/letsencrypt/live/yourdomain.com/*.pem /var/www/hi-catering/infra/nginx/ssl/ && docker compose -f /var/www/hi-catering/docker-compose.prod.yml restart nginx"
```

---

## 🆘 Что-то пошло не так?

### Контейнеры не запускаются

```bash
# Проверьте логи
docker compose -f docker-compose.prod.yml logs

# Проверьте, не заняты ли порты
sudo netstat -tulpn | grep -E ':(80|443)'
```

### SSL не работает

```bash
# Проверьте наличие сертификатов
ls -la infra/nginx/ssl/

# Проверьте конфигурацию Nginx
docker compose -f docker-compose.prod.yml exec nginx nginx -t
```

### База данных не работает

```bash
# Проверьте статус
docker compose -f docker-compose.prod.yml ps postgres

# Проверьте логи
docker compose -f docker-compose.prod.yml logs postgres
```

---

## 📚 Полная документация

Для детальной информации читайте:

- **[Быстрый старт](./docs/QUICK-START.md)** - Подробная 5-минутная инструкция
- **[Полное руководство](./docs/DEPLOYMENT.md)** - Все детали установки
- **[Выбор хостинга](./docs/SERVER-RECOMMENDATIONS.md)** - Какой сервер выбрать
- **[Шпаргалка](./docs/CHEATSHEET.md)** - Все команды в одном месте

---

## 🎯 Рекомендуемые хостинг-провайдеры

### 🇷🇺 Российские
- **Timeweb Cloud** - ~500₽/мес (хорошая поддержка)
- **Selectel** - ~600₽/мес (стабильный)
- **REG.RU VPS** - ~550₽/мес (простой)

### 🌍 Международные
- **Hetzner** - €5/мес (~500₽) - **ЛУЧШАЯ ЦЕНА**
- **DigitalOcean** - $24/мес (~2300₽) - простота
- **Vultr** - $24/мес (~2300₽) - быстрый

Подробнее о выборе хостинга: [SERVER-RECOMMENDATIONS.md](./docs/SERVER-RECOMMENDATIONS.md)

---

## 💡 Полезные команды

```bash
# Статус сервисов
docker compose -f docker-compose.prod.yml ps

# Просмотр логов
docker compose -f docker-compose.prod.yml logs -f

# Перезапуск
docker compose -f docker-compose.prod.yml restart

# Остановка
docker compose -f docker-compose.prod.yml down

# Резервная копия
./scripts/backup.sh

# Восстановление
./scripts/restore.sh

# Использование ресурсов
docker stats
```

Больше команд: [CHEATSHEET.md](./docs/CHEATSHEET.md)

---

## 📞 Нужна помощь?

1. Проверьте [документацию](./docs/README.md)
2. Посмотрите раздел ["Решение проблем"](./docs/DEPLOYMENT.md#решение-проблем)
3. Создайте issue в репозитории

---

**Удачного запуска! 🚀**

*Если у вас возникли вопросы, обратитесь к полной документации в папке `/docs/`*

