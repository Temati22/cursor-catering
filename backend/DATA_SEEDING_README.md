# Заполнение Strapi тестовыми данными

## 🚀 Быстрый старт

### 1. Заполнение данными (в Docker)

```bash
# Заполнить базу данных тестовыми данными
docker compose exec backend npm run seed:all

# Опубликовать все данные
docker compose exec backend npm run publish:all

# Перезапустить backend
docker compose restart backend
```

### 2. Настройка прав доступа через админку

После заполнения данными нужно настроить права доступа:

1. Откройте админку: http://localhost:1337/admin
2. Войдите под своим аккаунтом
3. Перейдите в **Settings** → **Users & Permissions** → **Roles** → **Public**
4. Включите следующие права для публичного доступа:

#### ✅ Что нужно включить:

**Category:**
- [x] find
- [x] findOne

**Dish:**
- [x] find
- [x] findOne

**Menu:**
- [x] find
- [x] findOne

**Event-page:**
- [x] find
- [x] findOne

**Service:**
- [x] find
- [x] findOne

**Advantage:**
- [x] find
- [x] findOne

**Global:**
- [x] find

**Contacts:**
- [x] find

5. Нажмите **Save** в правом верхнем углу

### 3. Проверка API

После настройки прав доступа проверьте API:

```bash
# Категории
curl http://localhost:1337/api/categories

# Блюда
curl http://localhost:1337/api/dishes

# Меню
curl http://localhost:1337/api/menus

# Страницы мероприятий
curl http://localhost:1337/api/event-pages

# Услуги
curl http://localhost:1337/api/services

# Преимущества
curl http://localhost:1337/api/advantages

# Глобальные настройки
curl http://localhost:1337/api/global

# Контакты
curl http://localhost:1337/api/contacts
```

## 📊 Что будет создано

### Категории (6 шт):
- Супы
- Салаты
- Горячие блюда
- Десерты
- Напитки
- Закуски

### Блюда (18 шт):
- Борщ с говядиной
- Солянка мясная
- Суп-пюре из грибов
- Цезарь с курицей
- Греческий салат
- Салат из свеклы с козьим сыром
- Стейк из говядины
- Лосось на пару с овощами
- Вегетарианская лазанья
- Куриные крылышки барбекю
- Тирамису
- Чизкейк Нью-Йорк
- Шоколадный фондан
- Латте
- Капучино
- Фреш из апельсинов
- Брускетты с томатами и моцареллой
- Канапе с семгой

### Меню (5 шт):
- Кофе-брейк "Классический"
- Обеденный сет "Бизнес"
- Свадебный банкет "Премиум"
- Детский день рождения
- Барбекю на природе

### Страницы мероприятий (3 шт):
- Корпоративные мероприятия
- Свадебный кейтеринг
- Дни рождения

### Услуги (3 шт):
- Кейтеринг на мероприятия
- Организация банкетов
- Выездное обслуживание

### Преимущества (4 шт):
- Качественные продукты
- Профессиональная команда
- Индивидуальный подход
- Доступные цены

### Глобальные настройки:
- Название сайта: "Hi Catering"
- Hero-секция с заголовками
- Тексты "О нас"

### Контакты:
- Телефоны: +7 (999) 123-45-67, +7 (999) 765-43-21
- Email: info@hicatering.ru
- Адрес: г. Москва, ул. Примерная, д. 123, офис 456
- Социальные сети

## 🛠️ Доступные команды

```bash
# Заполнение данными (локально, SQLite)
cd backend
npm run seed:all

# Заполнение данными (в Docker, PostgreSQL)
docker compose exec backend npm run seed:all

# Публикация всех данных
docker compose exec backend npm run publish:all

# Проверка данных в базе
docker compose exec backend node scripts/check-data.js
```

## ⚠️ Важно

1. **Скрипты нужно запускать внутри Docker контейнера** (с `docker compose exec backend`), если вы используете Docker
2. **После заполнения данными обязательно настройте права доступа** через админку
3. **Перезапустите backend** после настройки прав: `docker compose restart backend`
4. Если данные уже есть, скрипт создаст дубликаты (это нормально для тестирования)

## 🔧 Решение проблем

### API возвращает 404

**Причина:** Не настроены права доступа для публичного API

**Решение:** Настройте права доступа через админку (см. раздел "Настройка прав доступа")

### Данные не отображаются

**Причина:** Данные не опубликованы (в draft mode)

**Решение:** 
```bash
docker compose exec backend npm run publish:all
```

### Скрипт не находит изображения

**Причина:** Изображения должны быть в `backend/data/uploads/`

**Решение:** Скрипт использует существующие изображения из этой папки. Если их нет, данные создадутся без изображений.

## 📝 Примечания

- Все цены указаны в рублях
- Блюда содержат информацию об аллергенах, вегетарианстве, времени приготовления
- Меню связаны с блюдами через relations
- Все данные на русском языке
- TypeScript ошибки в логах не критичны и не влияют на работу

