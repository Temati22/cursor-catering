# 🖼️ Быстрая настройка галереи

## ✅ Что уже сделано:

### Backend (Strapi):
- ✅ Создан контент-тип `Gallery` с полями:
  - `title` - Заголовок
  - `description` - Описание
  - `images` - Галерея фотографий (множественная загрузка)
  - `SEO` - SEO метаданные
- ✅ Настроены контроллеры, маршруты и сервисы
- ✅ API endpoint: `/api/gallery`

### Frontend (Next.js):
- ✅ Создана страница `/galery`
- ✅ Добавлены типы TypeScript для Gallery
- ✅ Реализована функция API `getGallery()`
- ✅ Добавлены ссылки в Header и Footer
- ✅ Адаптивная сетка изображений
- ✅ Hover эффекты и оптимизация изображений

### Документация:
- ✅ Полная документация в `docs/GALLERY.md`
- ✅ Скрипт настройки прав доступа

## 🚀 Что нужно сделать:

### 1. ⚠️ Остановите Strapi (если запущен)

Если Strapi работает, остановите его (Ctrl+C в терминале где запущен `npm run develop`).

**ВАЖНО:** Скрипт настройки прав запускает свою копию Strapi!

### 2. Настройте права доступа

**Вариант А (Автоматически - РЕКОМЕНДУЕТСЯ):**

```bash
cd backend
npm run setup:gallery-permissions
```

Скрипт автоматически настроит права доступа к API галереи.

**Вариант Б (Настроить ВСЕ права сразу):**

```bash
cd backend
npm run setup:permissions
```

Это настроит права для всех контент-типов, включая Gallery.

**Вариант В (Вручную через админку):**

1. Запустите Strapi: `cd backend && npm run develop`
2. Откройте админ-панель: `http://localhost:1337/admin`
3. Перейдите: `Settings → Users & Permissions plugin → Roles → Public`
4. Найдите `Gallery` в списке
5. Включите галочку `find` ✅
6. Нажмите `Save`

### 3. Запустите Strapi

```bash
cd backend
npm run develop
```

### 4. Добавьте контент

1. В Strapi админке откройте: `Content Manager → Gallery`
2. Заполните поля:
   - **Title**: "Галерея Hi-Catering"
   - **Description**: "Фотографии наших мероприятий и блюд"
   - **Images**: Загрузите фотографии
3. Нажмите `Save`, затем `Publish`

### 5. Запустите frontend

```bash
cd apps/web
npm run dev
```

### 6. Откройте страницу

🌐 Страница доступна по адресу: **`http://localhost:3000/galery`**

## 📚 Дополнительная информация

Полная документация: [docs/GALLERY.md](docs/GALLERY.md)

## 🎨 Превью страницы

Страница включает:
- Breadcrumbs (Главная → Галерея)
- Hero секция с заголовком и описанием
- Адаптивная сетка изображений (1-3 колонки)
- Hover эффекты с подписями
- Оптимизация и lazy loading изображений
- SEO метаданные

## 📁 Созданные файлы

**Backend:**
```
backend/src/api/gallery/
├── content-types/gallery/schema.json
├── controllers/gallery.ts
├── routes/gallery.ts
└── services/gallery.ts

backend/scripts/
└── setup-gallery-permissions.js
```

**Frontend:**
```
apps/web/src/
├── app/galery/page.tsx
└── lib/api.ts (обновлен)
```

**Навигация (обновлено):**
```
apps/web/src/components/
├── Header.tsx
└── Footer.tsx
```

**Документация:**
```
docs/GALLERY.md
GALLERY_SETUP.md
```

## ✨ Готово!

После выполнения шагов 1-5, страница галереи будет полностью функциональна!

---

💡 **Совет:** Для лучшего результата загружайте изображения в высоком разрешении (1920x1080 или больше). Strapi автоматически создаст оптимизированные версии.

